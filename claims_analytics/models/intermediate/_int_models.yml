version: 2

models:
  - name: int_patients_enriched
    columns:
      - name: patient_id
        tests: [unique, not_null]

  - name: int_claims_with_encounters
    columns:
      - name: claim_id
        tests: [unique, not_null]

  - name: int_patient_chronic_conditions
    columns:
      - name: patient_id
